[package]
name = "unsaved_changes"
version = "0.1.0"
authors = ["Martin Kavík <martin@kavik.cz>"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
gloo-console = "0.3.0"
gloo-storage = "0.3.0"
seed = { path = "../../", features = ["routing"] }
